---
title: MCP Server
description: Connect Mikk to Claude Desktop, Cursor, and VS Code Copilot. 18 tools, 3 resources, millisecond responses - all grounded in your real codebase graph.
---

<Callout title="Every tool reads from mikk.lock.json" type="info">
  No re-parsing on every call. Response times are fast because the dependency graph, hashes, and indexes are precomputed and stored on disk.
</Callout>

## Quick Start

```bash
mikk mcp                          # start the server
mikk mcp install                  # auto-detect and install into Claude/Cursor/VS Code
mikk mcp install --tool claude    # install into Claude Desktop specifically
```

## Connecting AI Assistants

<Tabs items={["Claude Desktop", "Cursor", "VS Code"]}>
  <Tab value="Claude Desktop">
    Add to `%APPDATA%\Claude\claude_desktop_config.json` (Windows) or
    `~/Library/Application Support/Claude/claude_desktop_config.json` (macOS):

    ```json title="claude_desktop_config.json"
    {
      "mcpServers": {
        "mikk": {
          "command": "npx",
          "args": ["-y", "@getmikk/mcp-server", "/absolute/path/to/your/project"]
        }
      }
    }
    ```
  </Tab>
  <Tab value="Cursor">
    ```json title=".cursor/mcp.json"
    {
      "mcpServers": {
        "mikk": {
          "command": "npx",
          "args": ["-y", "@getmikk/mcp-server", "/absolute/path/to/your/project"]
        }
      }
    }
    ```
  </Tab>
  <Tab value="VS Code">
    ```bash
    mikk mcp install --tool vscode
    ```
  </Tab>
</Tabs>

## Staleness Warning (Context Drift)

If your lock file is out of sync with the source (drifted), tools return a `warning` field.
Refresh with `mikk analyze`, or keep it updated continuously with `mikk watch`.

## Tools Reference

### Project Overview

<TypeTable
  type={{
    mikk_get_project_overview: {
      description: "High-level overview: modules, function counts, file counts, tech stack.",
      type: "tool",
    },
    mikk_get_session_context: {
      description: "One-shot session start: overview, changes, hot modules, constraint status.",
      type: "tool",
    },
    mikk_get_changes: {
      description: "Files changed since the last analysis (drift list).",
      type: "tool",
    },
  }}
/>

### Understanding The Codebase

<TypeTable
  type={{
    mikk_query_context: {
      description: "Ask an architecture question. Returns graph-traced, token-budgeted context.",
      type: "tool",
    },
    mikk_list_modules: {
      description: "List all declared modules with file/function counts and entry points.",
      type: "tool",
    },
    mikk_get_module_detail: {
      description: "Deep module detail: files, functions, exported API, internal call graph.",
      type: "tool",
    },
    mikk_get_function_detail: {
      description: "Function detail: params, return type, call graph, source body, line range.",
      type: "tool",
    },
    mikk_search_functions: {
      description: "Fast substring search across function names (case-insensitive).",
      type: "tool",
    },
    mikk_semantic_search: {
      description: "Natural-language search for functions (local embeddings; optional dependency).",
      type: "tool",
    },
  }}
/>

### Safe Edits (Middleman Tools)

<TypeTable
  type={{
    mikk_before_edit: {
      description: "Call before editing any file. Returns blast radius, exported APIs at risk, constraints, and warnings.",
      type: "tool",
    },
    mikk_impact_analysis: {
      description: "Blast radius of changing a file. Includes risk breakdown and top impacted functions.",
      type: "tool",
    },
    mikk_find_usages: {
      description: "Find everything that calls a function (rename safely).",
      type: "tool",
    },
  }}
/>

### Reading Files

<TypeTable
  type={{
    mikk_get_file: {
      description: "Read raw source of a project file (path traversal guarded).",
      type: "tool",
    },
    mikk_read_file: {
      description: "Read raw source, scoped to specific functions to save tokens.",
      type: "tool",
    },
  }}
/>

### Constraints, ADRs, Health

<TypeTable
  type={{
    mikk_get_constraints: {
      description: "All architectural constraints and design decisions (from mikk.json).",
      type: "tool",
    },
    mikk_manage_adr: {
      description: "Manage ADRs (architecture decisions) stored in the project.",
      type: "tool",
    },
    mikk_dead_code: {
      description: "Detect dead code from the dependency graph and exports.",
      type: "tool",
    },
  }}
/>

### Routes

<TypeTable
  type={{
    mikk_get_routes: {
      description: "Detected HTTP routes (Express / Koa / Hono style).",
      type: "tool",
    },
  }}
/>

## Resources

| Resource | URI | Description |
|----------|-----|-------------|
| Contract | `mikk://contract` | Full `mikk.json` contract as JSON |
| Lock file | `mikk://lock` | Full `mikk.lock.json` as JSON |
| Context | `mikk://context` | Current `claude.md` content |

## Recommended Workflow

```
1) Start: mikk_get_session_context
2) Before editing: mikk_before_edit(["src/whatever.ts"])
3) Impact: mikk_impact_analysis({ file: "src/whatever.ts" })
4) Understanding: mikk_query_context({ question: "How does X work?", tokenBudget: 6000 })
```

<Callout title="Middleman > giant pastes" type="success">
  The lock file can be large. Instead of pasting it, keep it fresh with `mikk analyze` or `mikk watch`,
  then let the assistant call focused tools like `mikk_before_edit` and `mikk_impact_analysis` when it needs specifics.
</Callout>

