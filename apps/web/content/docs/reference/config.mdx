---
title: mikk.json Reference
description: Complete schema reference for the mikk.json architecture contract file — modules, constraints, ADRs, and safety policies.
---

<Callout type="info">
  `mikk.json` is generated by `mikk init`. Edit it to define your module boundaries, constraints,
  architectural decisions, and safety policy thresholds.
</Callout>

## Top-level Schema

<TypeTable
  type={{
    version: {
      description: "Contract schema version.",
      type: "string",
      required: true,
    },
    project: {
      description: "Project metadata used for generated artifacts (claude.md, AGENTS.md).",
      type: "object",
      required: true,
    },
    declared: {
      description: "Declared modules, constraints, and ADR decisions.",
      type: "object",
      required: true,
    },
    overwrite: {
      description: "Controls how mikk init/analyze handles existing mikk.json fields.",
      type: "object",
      default: "{ mode: 'never', requireConfirmation: true }",
    },
    policies: {
      description: "Safety gate thresholds and enforcement toggles used by the Decision Engine and Intent Engine.",
      type: "object",
      default: "{ maxRiskScore: 70, maxImpactNodes: 10 }",
    },
  }}
/>

## ProjectDefinition

<TypeTable
  type={{
    name: {
      description: "Project name used in generated claude.md and AGENTS.md.",
      type: "string",
      required: true,
    },
    description: {
      description: "Short project description for AI context.",
      type: "string",
      required: true,
    },
    language: {
      description: "Primary language for parser selection.",
      type: '"typescript" | "javascript" | "go" | "python" | "java" | "csharp" | "rust" | "cpp" | "c" | "php" | "ruby"',
      required: true,
    },
    framework: {
      description: "Optional framework hint used for AI context labels.",
      type: "string",
    },
    entryPoints: {
      description: "Optional list of entry point file paths for context.",
      type: "string[]",
    },
  }}
/>

## ModuleDefinition

<TypeTable
  type={{
    id: {
      description: "Module ID — used in constraints, graph output, and policy matching. Keep short and lowercase.",
      type: "string",
      required: true,
    },
    name: {
      description: "Human-readable module name used in AI context.",
      type: "string",
      required: true,
    },
    description: {
      description: "Module description used as context for AI queries.",
      type: "string",
      required: true,
    },
    intent: {
      description: "Optional AI-facing intent description — surfaced in mikk_query_context.",
      type: "string",
    },
    owners: {
      description: "Optional team or owner labels.",
      type: "string[]",
    },
    paths: {
      description: "File glob patterns that belong to this module. Module assignment uses the most-specific matching pattern.",
      type: "string[]",
      required: true,
    },
    entryFunctions: {
      description: "Optional list of exported function names that form the public API of this module.",
      type: "string[]",
    },
  }}
/>

## Policies Schema

<TypeTable
  type={{
    maxRiskScore: {
      description: "WARN when impact risk score exceeds this value. BLOCK is always triggered at ≥ 90 regardless of this setting.",
      type: "number",
      default: "70",
    },
    maxImpactNodes: {
      description: "WARN when impacted function count exceeds this value. BLOCK at maxImpactNodes × 2.",
      type: "number",
      default: "10",
    },
    protectedModules: {
      description: "Module IDs that can never be touched without explicit architecture review. Matches by checking if a node's file path contains the module ID string. Never bypassable.",
      type: "string[]",
      default: "[]",
    },
    enforceStrictBoundaries: {
      description: "BLOCK on any critical-classified cross-module impact (depth=1 and different module).",
      type: "boolean",
      default: "false",
    },
    requireTestsForChangedFiles: {
      description: "BLOCK high-risk changes (exported functions with many callers) if no test files are also modified. Bypassable.",
      type: "boolean",
      default: "true",
    },
    requireDocumentationForApiChanges: {
      description: "BLOCK significant API changes (exported + > 5 callers) if no README.md, API.md, CHANGELOG.md, or AGENTS.md is updated. Bypassable.",
      type: "boolean",
      default: "false",
    },
  }}
/>

## ADR Schema

<TypeTable
  type={{
    id: {
      description: "Unique identifier, e.g. ADR-001.",
      type: "string",
      required: true,
    },
    title: {
      description: "Short human-readable title for the decision.",
      type: "string",
      required: true,
    },
    date: {
      description: "Date the decision was made (ISO 8601 or YYYY-MM-DD).",
      type: "string",
    },
    reason: {
      description: "Why this decision was made. Surfaced in every mikk_query_context response.",
      type: "string",
      required: true,
    },
  }}
/>

## Full Example

```json title="mikk.json"
{
  "version": "1.0.0",
  "project": {
    "name": "my-project",
    "description": "Full-stack TypeScript API with Next.js frontend.",
    "language": "typescript",
    "framework": "nextjs",
    "entryPoints": ["src/server.ts"]
  },
  "declared": {
    "modules": [
      {
        "id": "auth",
        "name": "Authentication",
        "description": "JWT auth and session management",
        "intent": "Handle user authentication and session lifecycle",
        "owners": ["security-team"],
        "paths": ["src/auth/**"],
        "entryFunctions": ["login", "logout", "validateToken"]
      },
      {
        "id": "payments",
        "name": "Payments",
        "description": "Payment processing and billing via Stripe",
        "paths": ["src/payments/**"]
      },
      {
        "id": "shared",
        "name": "Shared Utilities",
        "description": "Shared helpers, validators, and type definitions",
        "paths": ["src/shared/**", "src/utils/**"]
      }
    ],
    "constraints": [
      "auth must not import from payments",
      "payments can only import from shared, db",
      "module:api cannot import from db directly"
    ],
    "decisions": [
      {
        "id": "ADR-001",
        "title": "Stateless JWT authentication",
        "reason": "Avoids session storage in distributed deployments. Access tokens are short-lived (15 min), refresh tokens stored in HttpOnly cookies.",
        "date": "2025-01-15"
      },
      {
        "id": "ADR-002",
        "title": "Payments module is isolated",
        "reason": "PCI DSS compliance requires strict boundary isolation for payment logic. No other module may import from payments.",
        "date": "2025-02-01"
      }
    ]
  },
  "overwrite": {
    "mode": "never",
    "requireConfirmation": true
  },
  "policies": {
    "maxRiskScore": 70,
    "maxImpactNodes": 10,
    "protectedModules": ["auth", "payments"],
    "enforceStrictBoundaries": true,
    "requireTestsForChangedFiles": true,
    "requireDocumentationForApiChanges": false
  }
}
```

## Constraint Syntax Reference

All constraints are plain strings. The boundary checker parses them at runtime.

| Pattern | Meaning |
|---------|---------|
| `A must not import from B` | A cannot have import edges to any file in B |
| `A can only import from B, C` | A may only import from B and C |
| `A is isolated` | A has no cross-module imports |
| `module:A cannot import B` | Explicit `module:` prefix form |
| `module:A has no imports` | A has zero cross-module dependencies |

Constraint matching is **case-insensitive** on the module name portion.
