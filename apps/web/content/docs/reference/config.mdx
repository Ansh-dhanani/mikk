---
title: mikk.json Reference
description: Complete schema reference for the mikk.json architecture contract file.
---

<Callout type="info">
  `mikk.json` is generated by `mikk init` and `mikk contract generate`. Edit it to define your module boundaries, constraints, and architectural decisions.
</Callout>

## Top-level Schema

<TypeTable
  type={{
    modules: {
      description: "Map of module IDs to module definitions. The core of the contract.",
      type: "Record<string, ModuleDefinition>",
      required: true,
    },
    decisions: {
      description: "Architecture Decision Records (ADRs) tracking significant choices.",
      type: "ADR[]",
      default: "[]",
    },
    config: {
      description: "Global configuration options for Mikk's analysis behavior.",
      type: "MikkConfig",
      default: "{}",
    },
  }}
/>

## ModuleDefinition

<TypeTable
  type={{
    intent: {
      description: "Human-readable description of the module's purpose. Used for AI context building.",
      type: "string",
      required: true,
    },
    include: {
      description: "Glob patterns for files that belong to this module.",
      type: "string[]",
      required: true,
    },
    publicApi: {
      description: "Exported function names that form this module's public surface.",
      type: "string[]",
      default: "[]",
    },
    constraints: {
      description: "Architectural rules enforced by mikk contract validate.",
      type: "Constraints",
      default: "{}",
    },
  }}
/>

## Constraints

<TypeTable
  type={{
    "no-import": {
      description: "Module IDs that this module cannot import from.",
      type: "string[]",
    },
    "must-use": {
      description: "Module IDs that this module must import from.",
      type: "string[]",
    },
    "no-call": {
      description: "Module IDs whose functions this module cannot call.",
      type: "string[]",
    },
    layer: {
      description: "Layer number. Higher-layer modules cannot import from lower-layer modules.",
      type: "number",
    },
    naming: {
      description: "Regex pattern that all exported function names in this module must match.",
      type: "string",
    },
    "max-files": {
      description: "Maximum number of files allowed in this module.",
      type: "number",
    },
  }}
/>

## Full Example

```json title="mikk.json"
{
  "modules": {
    "core": {
      "intent": "Pure business logic — no I/O, no framework dependencies",
      "include": ["src/core/**"],
      "publicApi": ["processOrder", "validateCart", "calculateTax"],
      "constraints": {
        "no-import": ["api", "db", "auth"],
        "layer": 1,
        "naming": "^process|^validate|^calculate"
      }
    },
    "auth": {
      "intent": "Authentication and session management",
      "include": ["src/auth/**"],
      "publicApi": ["login", "logout", "validateToken", "refreshSession"],
      "constraints": {
        "no-import": ["payments"],
        "must-use": ["logger"],
        "layer": 2
      }
    },
    "api": {
      "intent": "HTTP layer — route handlers and middleware only",
      "include": ["src/api/**"],
      "publicApi": [],
      "constraints": {
        "no-call": ["db"],
        "layer": 3,
        "max-files": 30
      }
    },
    "db": {
      "intent": "Database access layer — queries and migrations",
      "include": ["src/db/**"],
      "publicApi": ["query", "transaction", "migrate"],
      "constraints": {
        "no-import": ["api", "auth"],
        "layer": 1
      }
    }
  },
  "decisions": [
    {
      "id": "ADR-001",
      "title": "Core module has zero framework dependencies",
      "status": "accepted",
      "date": "2025-01-15",
      "rationale": "Keep business logic testable without spinning up HTTP or DB",
      "constraints": ["core.no-import.api", "core.no-import.db"]
    }
  ],
  "config": {
    "ignorePatterns": ["**/*.test.ts", "**/*.spec.ts"],
    "maxHops": 3,
    "defaultTokenBudget": 4000
  }
}
```

## MikkConfig

<TypeTable
  type={{
    ignorePatterns: {
      description: "Additional glob patterns to exclude from analysis (supplementing .mikkignore).",
      type: "string[]",
      default: "[]",
    },
    maxHops: {
      description: "Default BFS depth for context building.",
      type: "number",
      default: "2",
    },
    defaultTokenBudget: {
      description: "Default token budget for mikk context build.",
      type: "number",
      default: "4000",
    },
  }}
/>

## ADR Schema

<TypeTable
  type={{
    id: {
      description: "Unique identifier, e.g. ADR-001.",
      type: "string",
      required: true,
    },
    title: {
      description: "Short human-readable title for the decision.",
      type: "string",
      required: true,
    },
    status: {
      description: "Current status of the decision.",
      type: '"proposed" | "accepted" | "deprecated" | "superseded"',
      required: true,
    },
    date: {
      description: "Date the decision was made (ISO 8601).",
      type: "string",
    },
    rationale: {
      description: "Why this decision was made.",
      type: "string",
    },
    constraints: {
      description: "Constraint rule IDs that enforce this decision (e.g. auth.no-import.payments).",
      type: "string[]",
    },
  }}
/>
